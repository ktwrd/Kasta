@using kate.FileShare.Data
@using kate.FileShare.Data.Models
@inject ApplicationDbContext _db

<table class="table">
    <thead>
        <tr>
            <th>Table</th>
            <th>Id</th>
            <th>By</th>
            <th>At</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var master in _db.Audit)
        {
            <tr>
                <th>@master.EntityName</th>
                <td><code>@master.PrimaryKey</code></td>
                <td>@(master.CreatedByUser.UserName)</td>
                <td>@(master.CreatedAt.ToString("s"))</td>
            </tr>
        }
    </tbody>
</table>